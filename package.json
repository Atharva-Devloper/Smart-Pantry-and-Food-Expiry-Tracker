{
  "name": "smart-pantry-and-food-expiry-tracker",
  "version": "1.0.0",
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "start": "npm run dev",
    "server": "cd server && npm run dev",
    "client": "cd client && npm run dev",
    "build": "cd client && npm run build",
    "install-deps": "npm install && cd client && npm install && cd ../server && npm install",
    "test": "echo \"No tests configured\" && exit 1"
  },
  "keywords": [
    "pantry",
    "food",
    "expiry",
    "tracker",
    "react",
    "express"
  ],
  "author": "",
  "license": "ISC",
  "description": "A smart pantry and food expiry tracker application with React frontend and Express backend",
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
